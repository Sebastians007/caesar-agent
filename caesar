#!/usr/bin/env python3
"""
Caesar Agent CLI launcher.

This wrapper should behave like the installed `caesar` command, including
subcommands such as `gateway`, `cron`, and `doctor`.
"""

if __name__ == "__main__":
    from caesar_cli.main import main
    main()
